{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dictation-widget",
  "description": "Least-privilege capability for the transparent dictation overlay: it may listen for widget-update events and read the current hotkey, nothing else",
  "windows": ["dictation-widget"],
  "permissions": [
    "dictation-widget-commands",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
